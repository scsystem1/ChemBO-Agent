# ChemBO Agent — Phase 1 Demo Dependencies
# ==========================================

# --- Core Agent Framework ---
langgraph>=0.3.0
langchain-core>=0.3.0
langchain-anthropic>=0.3.0
langchain-openai>=0.2.0

# --- Bayesian Optimization Engine ---
# Validated in the dedicated `chembo` Python 3.11 environment.
torch>=2.10,<2.11
gpytorch>=1.15,<1.16
botorch>=0.17,<0.18

# --- Chemistry ---
rdkit-pypi       # Molecular fingerprints, SMILES handling

# --- Embeddings ---
openai>=1.30.0             # text-embedding-3-large API
# transformers>=4.40.0     # (optional) ChemBERTa — uncomment if needed

# --- Data & Config ---
# RDKit wheels in common Python environments are often still built against
# the NumPy 1.x ABI, so pin below 2.0 for the stable default install path.
numpy>=1.26,<2
pandas>=2.2.0
scipy>=1.12.0
scikit-learn>=1.4.0
pyyaml>=6.0
pydantic>=2.6.0

# --- Storage (Phase 1: in-memory; extendable) ---
# qdrant-client>=1.9.0     # (optional) vector DB for episodic memory
# neo4j>=5.19.0            # (optional) knowledge graph

# --- Utilities ---
rich>=13.7.0               # pretty terminal output
pytest>=8.0.0
requests>=2.31.0

# --- Knowledge / RAG ---
chromadb>=0.5.0
sentence-transformers>=3.0.0
pymupdf>=1.24.0
rank-bm25>=0.2.2
tavily-python>=0.5.0
